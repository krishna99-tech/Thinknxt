/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Optional: Add turbopack config if needed
  // experimental: {
  //   turbo: {
  //     rules: {}
  //   }
  // }
}

module.exports = nextConfig